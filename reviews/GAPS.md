# Gap Ledger

Running, cross-review ledger of every finding raised in `reviews/REVIEW-NNN-*.md` files. See `AGENTS.md` §
"Review Findings and Gap Tracking" for the convention this file follows. Rows are never deleted — a `CLOSED`
row is proof a gap was found and fixed, not evidence it can be forgotten.

**Status values:** `OPEN` (not yet addressed) · `IN_PROGRESS` (someone is actively working it) · `CLOSED` (fixed,
with commit hash) · `ACCEPTED` (consciously deferred with a documented reason — only valid for `MEDIUM`/`LOW`
severity; `CRITICAL`/`HIGH` gaps may not be marked `ACCEPTED` in lieu of fixing them without also revising the
source SPEC/ADR that they violate).

**Gate rule:** no phase may be declared complete in `docs/NEXT_STEPS.md` or have its `specs/SPEC-10-phase-plan.md`
checklist ticked while any `CRITICAL` or `HIGH` row tied to that phase is still `OPEN`.

| Gap ID | Severity | Summary | Source | File(s) | Status | Closed By |
|---|---|---|---|---|---|---|
| GAP-001 | CRITICAL | Self-approval prevention unenforced: `PermissionService` exists but is never called by `ApprovalService`/`POST /approvals`; no field records who proposed a task, so self-approval can't even be detected once wired | REVIEW-001 | `services/permission_service.py`, `services/approval_service.py`, `api/approvals.py`, `models/task.py` | CLOSED | `08f1171` |
| GAP-002 | CRITICAL | `PolicyEngine` forbidden-path check was inverted (fixed in `94444b2`), but the replacement checked exact string equality only. Now uses path-prefix/containment comparison, so `~/.ssh/id_rsa` is rejected when `~/.ssh` is forbidden | REVIEW-001, REVIEW-002 | `services/policy_engine.py` | CLOSED | `14d11da` |
| GAP-003 | HIGH | `Idempotency-Key` HTTP header (SPEC-03 §3.3 primary mechanism) is never read; endpoint always computes the fallback synthetic key regardless of whether a header was supplied | REVIEW-001 | `api/approvals.py:41-46` | CLOSED | `70948bb` |
| GAP-004 | HIGH | The (synthetic or real) idempotency key is never used for approval dedup lookup — dedup is keyed on `(task_id, approval_type, actor)` only, so distinct approval attempts can silently collapse into a no-op | REVIEW-001 | `services/approval_service.py:99-105` | CLOSED | `70948bb` |
| GAP-005 | HIGH | Response status codes don't match SPEC-03 §3.3: policy violations return 422 instead of 403; 400 is never emitted; a malformed `timestamp` raises an unguarded exception → unhandled 500 | REVIEW-001 | `api/approvals.py:28,109-112` | CLOSED | `fd6501b` |
| GAP-006 | HIGH | `2edda61` wired `CompletionContract`'s static checks, but `required`/`optional` commands were never executed and `VerificationService` was not called from any live path. Now `VerificationService` executes commands via subprocess, compares exit codes, and is invoked by `EventBridge.handle()` on `landing:completed` to gate `AGENT_REVIEW → HUMAN_REVIEW` | REVIEW-001, REVIEW-002 | `services/verification_service.py`, `adapters/macro_agent/event_bridge.py` | CLOSED | `116d95a` |
| GAP-007 | HIGH | Most `ProjectProfile` security/git fields (`docker_socket`, `destructive_shell`, `spawn_subagents`, `network`, `force_push`, `signed_commits`) are schema-only and never enforced by `PolicyEngine` | REVIEW-001 | `schemas/project_profile.py`, `services/policy_engine.py` | CLOSED | `ae766eb` |
| GAP-008 | HIGH | `c1195d8`'s "reconciliation stub" was orphaned dead code: never invoked by anything, compared against inert never-populated data, and implemented ~1 of SPEC-03 §3.9's 5 requirements. Removed and explicitly deferred to Phase 2 in `docs/NEXT_STEPS.md` until Plane and opentasks are real integrations | REVIEW-001, REVIEW-002 | `services/reconciliation_service.py`, `docs/NEXT_STEPS.md` | CLOSED | `ff10d89` |
| GAP-009 | MEDIUM | `5af189c`'s `docs/DEPENDENCIES.md` falsely claimed `structlog`+`python-json-logger` were paired via a JSON console processor. Dropped the unused `python-json-logger` dependency and corrected the doc | REVIEW-001, REVIEW-002 | `src/governance_controller/docs/DEPENDENCIES.md`, `pyproject.toml` | CLOSED | `4b3411c` |
| GAP-010 | MEDIUM | `GET /executions/{id}` always returns 501 with a docstring claiming execution records aren't persisted yet — false, `Execution` rows are created by `ApprovalService._trigger_execution` | REVIEW-001 | `api/executions.py` | CLOSED | `d67df76` |
| GAP-011 | MEDIUM | `docs/NEXT_STEPS.md` claims an "audit-log endpoint" exists; no such HTTP route exists anywhere — only the internal `AuditService`/`AuditLog` model | REVIEW-001 | `docs/NEXT_STEPS.md`, `api/` | CLOSED | `e28c152` |
| GAP-012 | MEDIUM | `aa6a82b` added real `AuditService.log` calls on task/profile creation, but `project_profile_updated` fired unconditionally and had no tests. Now only logs `project_profile_updated` when profile content changes, and tests cover task creation, profile creation, idempotent reuse, and content-change updates | REVIEW-001, REVIEW-002 | `services/task_service.py`, `tests/test_task_service_audit.py` | CLOSED | `fa94ad5` |
| GAP-013 | MEDIUM | `31b6dfa` ticked the checklist with honest footnoted deferrals for 5 of 6 items, but ticked "Failed verification never produces DONE" with no disclosure that `VerificationService` is unwired (see GAP-006). Corrected directly in REVIEW-002 (line unchecked + footnote added) | REVIEW-001, REVIEW-002 | `specs/SPEC-10-phase-plan.md` | CLOSED | REVIEW-002 (direct doc fix) |
| GAP-014 | MEDIUM | `EventBridge.handle()` silently swallows invalid-transition errors (`suppress(ValueError)`) and logs an audit entry indistinguishable from a successful transition, reducing audit fidelity | REVIEW-001 | `adapters/macro_agent/event_bridge.py` | CLOSED | `ca838c8` |
| GAP-015 | LOW | `test_placeholder.py` is vestigial scaffold (`assert True`), safe to delete now that real tests exist | REVIEW-001 | `tests/test_placeholder.py` | CLOSED | `d05dfb5` |
| GAP-016 | LOW | `08f1171` committed the 23 previously-untracked task brief/report files, but `progress.md` only checked off Tasks 1-2 and didn't list Tasks 17-20. Updated `progress.md` with all 20 tasks and the review gap-closure follow-ups | REVIEW-001, REVIEW-002 | `.superpowers/sdd/2026-08-18-phase-1-governance-controller-poc/progress.md` | CLOSED | `c088c7e` |
| GAP-017 | LOW | `Settings` uses deprecated Pydantic v1-style `class Config:` instead of `model_config = SettingsConfigDict(...)` | REVIEW-001 | `governance_controller/config.py` | CLOSED | `060c7c8` |
| GAP-018 | LOW | Hardcoded local-dev DB credentials (`postgres:postgres`, `controller:controller`) with no comment marking them as dev-only defaults | REVIEW-001 | `governance_controller/config.py`, `docker-compose.yml` | CLOSED | `b996ad1` |
| GAP-019 | LOW | `api/__init__.py` re-exports `approvals`/`executions`/`tasks` but not `health`; cosmetic only, no functional bug | REVIEW-001 | `api/__init__.py` | CLOSED | `e28c152` |
| GAP-020 | HIGH | `VerificationService`'s own `forbidden_path_check` (distinct from `PolicyEngine`'s, fixed by GAP-002/`14d11da`) still uses exact-string set intersection — `inputs=["~/.ssh/id_rsa"]` against `forbidden_path_check.paths=["~/.ssh"]` is not caught, same vulnerability class as GAP-002, in the sibling `verification_service.py`, one function away from a correct `_is_prefixed_by()` helper the same commit added and used for `scope_check` but not here | REVIEW-003 | `services/verification_service.py` (`forbidden_path_check` block) | CLOSED | `1f8f588` |
| GAP-021 | MEDIUM | The `landing:completed` → `EventBridge` → `VerificationService.verify_and_advance` failure-gating path (failing required check → `FAILED`, not `HUMAN_REVIEW`) is correctly implemented and confirmed by live reproduction. `5b157b8` added a real integration test for the *failing* half through `EventBridge.handle()`. The *passing* half (→ `HUMAN_REVIEW`) is still only unit-tested directly against `VerificationService`, never through the real `EventBridge` path | REVIEW-003, REVIEW-005 | `tests/test_event_bridge.py` | CLOSED | `9d0bd32` |
| GAP-022 | CRITICAL | `session.commit()` was never called anywhere — every write was discarded past one request against real PostgreSQL. `get_db()` now commits on success / rolls back on exception; proven correct by direct reproduction against real session/connection semantics (not test fixtures) in REVIEW-005. Genuinely fixed — see `GAP-043` for the separate test-coverage gap this left behind | REVIEW-004, REVIEW-005 | `governance_controller/db.py`, all services | CLOSED | `cb7e0ce` |
| GAP-023 | HIGH | The claimed fix (row lock + optimistic `version` guard) did not work: `AsyncSessionLocal`'s `expire_on_commit=False` plus missing `populate_existing=True`/`session.refresh()` meant the "re-fetch under lock" returned the *same cached Python object* already held by the caller, so `locked_task.version != task.version` could never be `True`. `.with_for_update()` is also a silent no-op on SQLite (the test default). Replaced the guard with an atomic `UPDATE task SET state=..., version=version+1 WHERE id=? AND version=? AND state=?`; if the rowcount is 0, another approval already advanced the task and `Concurrent modification detected` is raised. Added deterministic tests proving a stale read followed by a committed advance is rejected | REVIEW-004, REVIEW-005 | `services/approval_service.py`, `services/state_machine.py`, `models/task.py`, `tests/test_approval_concurrency.py` | CLOSED | `e96dedf` |
| GAP-024 | HIGH | `1cacd89` genuinely fixed the `completion_contract` path — `PolicyEngine` now inspects actual `Check.command` content (denylist for destructive/docker-socket/shell-metacharacter patterns) and catches a contract that lies about `destructive_shell=false` while its command is `rm -rf ~`, confirmed by live execution. **But** `TaskContract.verification.commands` (the SPEC-03 §3.5 field GAPS.md claimed was "wired into VerificationService") was read by `VerificationService` and executed via raw `asyncio.create_subprocess_shell` with **zero `PolicyEngine` validation** — proven live by executing an unchecked marker command through this exact field. Same vulnerability class, different field, zero test coverage. Now `PolicyEngine.evaluate()` also validates commands in `TaskContract.verification["commands"]` using the same logic as `CompletionContract` commands | REVIEW-004, REVIEW-005 | `services/policy_engine.py`, `services/verification_service.py` | CLOSED | `fb53778` |
| GAP-025 | HIGH | `TelegramAdapter.process_update()` has no webhook authentication (no secret-token/HMAC/IP-allowlist check) and hardcodes `actor="telegram-user"` as a literal string, never derived from the sender — currently unrouted dead code, but one FastAPI route away from being a direct, unauthenticated, unattributable path to `POST /approvals` | REVIEW-004 | `adapters/telegram.py` | CLOSED | `746988b` |
| GAP-026 | HIGH | `EventBridge.handle()` has no idempotency/replay protection keyed on `(task_id, event_type, event_timestamp, event_id)` as SPEC-05 §5.4 requires; the codebase's own `test_duplicate_event_is_idempotent` explicitly locks in that a replayed event produces 2 audit rows for 1 logical event. SPEC-05 §5.4 anticipates retries as the routine failure-recovery path, not an edge case | REVIEW-004 | `adapters/macro_agent/event_bridge.py` | CLOSED | `1887c4c` |
| GAP-027 | HIGH | `docs/NEXT_STEPS.md` claimed Phase 1 "complete and fully reviewed" / all findings `CLOSED` while `GAP-020` (HIGH) was still `OPEN`, contradicting this ledger's own gate rule | REVIEW-004 | `docs/NEXT_STEPS.md` | CLOSED | REVIEW-004 (direct doc fix) |
| GAP-028 | MEDIUM | `mypy --strict` (configured in `pyproject.toml`, never actually run/reported in REVIEW-001/002/003) surfaces 38 errors in 17 files. Most are cosmetic (`dict` → `dict[str, Any]`, missing SQLAlchemy mypy plugin), but `api/health.py`'s degraded-path branch returns a raw `JSONResponse` from a function typed `-> HealthResponse` — a genuine, if currently harmless, type-contract violation | REVIEW-003 (found by round-4 test/tooling reviewer, logged under REVIEW-004) | `governance_controller/**`, esp. `api/health.py:31`, `db.py:15` | CLOSED | `9cea0ff` |
| GAP-029 | MEDIUM | `GitCascadeService` (`land()`, `validate_branch_name()`) is orphaned — never called from any live code path, same pattern as the already-fixed GAP-008 (reconciliation). Honestly disclosed as a stub in `docs/NEXT_STEPS.md`/SPEC-10 footnotes, so not a documentation-accuracy issue, but worth a decision: wire it into `landing:completed` now that `VerificationService` is wired, or remove it | REVIEW-004 | `services/git_cascade.py` | CLOSED | `0920b65` |
| GAP-030 | MEDIUM | `GitCascadeService.validate_branch_name`'s regex (`^[a-zA-Z0-9_.-]+$`) permits a leading `-`, so branch names like `--upload-pack=...` pass validation — a latent git/ssh argument-injection gate weakness if `land()` is ever made to shell out to real `git` (currently it doesn't) | REVIEW-004 | `services/git_cascade.py` | CLOSED | `0920b65` |
| GAP-031 | MEDIUM | Happy-path fix genuinely works (`005f9b0`, confirmed REVIEW-006): `Execution` row created before `executor.start(contract, execution.id)`, real id propagates into the outbound `metadata.controller_execution_id`. The specific defect this row describes — `executor.start()` failure raising `RuntimeError` uncaught, reaching `get_db()`'s rollback, and erasing the Execution row/FAILED marking/audit entries for that request — is fixed: `GAP-044`'s commit-before-raise fix (`3e93928`) makes all of that survive rollback, independently reproduced in REVIEW-010 and re-confirmed in REVIEW-013 by 3 independent reviewers. Ledger status was simply never updated after the fix landed. **Note:** the distinct, narrower residual — the uncaught `RuntimeError` still surfaces to the client as a bare 500 rather than a mapped status code — is real but was never what this row described; tracked separately as `GAP-064` | REVIEW-004, REVIEW-005, REVIEW-006, REVIEW-013 | `services/approval_service.py`, `api/approvals.py`, `governance_controller/db.py` | CLOSED | `3e93928` (ledger corrected in REVIEW-013) |
| GAP-032 | MEDIUM | `0920b65` corrected `allowed_roles` *values* to match SPEC-06 §6.2 (Claude Code: planner/architect/reviewer, no worker; OpenCode: worker/reviewer/planner/meta) — but the field was still read nowhere in the codebase; `PolicyEngine` only checked `allowed_harnesses`, never `allowed_roles`. Added `role: str = "worker"` to `ExecutionConfig` and made `PolicyEngine.evaluate()` look up the harness in the registry and reject a role not in `provider.allowed_roles` | REVIEW-004, REVIEW-005 | `harness/base.py`, `harness/claude_code.py`, `harness/opencode.py`, `services/policy_engine.py`, `schemas/task_contract.py` | CLOSED | `7902151` |
| GAP-033 | MEDIUM | `tests/test_task_api.py` has only 2 tests and never exercises the 404 branch `api/tasks.py` explicitly implements — every sibling API test file (`test_executions_endpoint.py`, `test_audit_endpoint.py`, `test_health_endpoint.py`) does test its error branch; this one is the outlier | REVIEW-004 | `tests/test_task_api.py` | CLOSED | `93f9af6` |
| GAP-034 | MEDIUM | Docker image (`Dockerfile`) has no `USER` directive — the API process runs as root inside the container | REVIEW-004 | `src/governance_controller/Dockerfile` | CLOSED | `91c3a4b` |
| GAP-035 | MEDIUM | No `RISK` row in `requirements/REQUIREMENTS.md` covered the live, unmitigated shell-execution-of-agent-controlled-command concern (see GAP-024) — added as `RISK-14` | REVIEW-004 | `requirements/REQUIREMENTS.md` | CLOSED | REVIEW-004 (direct doc fix, RISK-14) |
| GAP-036 | MEDIUM | `specs/SPEC-10-phase-plan.md`'s `[^phase1-verification]` footnote described the pre-GAP-006 (unwired, hardcoded-`"passed"`) state, which GAP-006's fix made factually wrong | REVIEW-004 | `specs/SPEC-10-phase-plan.md` | CLOSED | REVIEW-004 (direct doc fix) |
| GAP-037 | MEDIUM | `README.md`'s Repository Layout table still marked `src/`/`tests/` as `(future)` despite `src/governance_controller/` being a real, substantial subproject with 49 production files and 134 tests | REVIEW-004 | `README.md` | CLOSED | REVIEW-004 (direct doc fix) |
| GAP-038 | LOW | `state_machine.py` allows `HUMAN_REVIEW -> RUNNING`, a transition not in SPEC-03 §3.2's diagram and never invoked anywhere — an unguarded escape hatch a future "send back for rework" feature could wire up without adding a policy/permission check | REVIEW-004 | `services/state_machine.py` | CLOSED | `3fc7d55` |
| GAP-039 | LOW | `ProjectProfile.llm`/`ProjectProfile.audit` remain schema-only/unenforced — outside GAP-007's fix scope, and there's currently no `TaskContract` field carrying a requested model to check `llm.allowed_models` against | REVIEW-004 | `schemas/project_profile.py` | ACCEPTED — no TaskContract field exists yet to enforce this against; revisit if/when model selection is added to the Task Contract | |
| GAP-040 | LOW | `adapters/macro_agent/client.py`'s HTTP calls rely on httpx's implicit 5s default timeout rather than an explicit, configurable one | REVIEW-004 | `adapters/macro_agent/client.py` | CLOSED | `2b22f1b` |
| GAP-041 | LOW | `.dockerignore` doesn't exclude `.env`-shaped files | REVIEW-004 | `src/governance_controller/.dockerignore` | ACCEPTED — no `.env` file exists anywhere in the project; `config.py` reads only from process environment via `pydantic-settings`, no dotenv loading | |
| GAP-042 | LOW | `docker-compose.yml` publishes Postgres port `5432` to the host beyond what the compose-internal network needs | REVIEW-004 | `src/governance_controller/docker-compose.yml` | ACCEPTED — intentional for local `psql` access during dev; credentials already clearly dev-only-commented per GAP-018 | |
| GAP-043 | MEDIUM | `2d5ed2a` changed `get_db()` to catch `BaseException` and added `test_db.py` tests for commit/rollback. REVIEW-006's mutation testing showed the original tests didn't distinguish `except BaseException` from `except Exception` because they exited via `GeneratorExit`. Added `test_get_db_commit_branch_requires_exception_catch` which throws a `ValueError` into the live generator with `gen.athrow()`; with `except Exception:` the rollback would not run and the inserted row would survive, but with `except BaseException:` it is rolled back as required | REVIEW-005, REVIEW-006 | `tests/test_db.py`, `governance_controller/db.py` | CLOSED | `6fc88ed` |
| GAP-044 | HIGH | Fourth attempt (`3e93928`) genuinely fixes the code: all three failure branches inside `_trigger_execution` (READY-CAS loss, executor-crash/FAILED-CAS, RUNNING-CAS loss) now commit immediately before their `raise`. Independently reproduced against the real, unmodified `get_db()` for all four scenarios — the EXEC_APPROVED transition, Execution row, and each branch's own audit row all survive a real rollback cycle. See `GAP-056` for a real, separate problem found while verifying this: the tests written to protect it don't actually exercise the rollback path | REVIEW-007, REVIEW-008, REVIEW-009, REVIEW-010 | `services/approval_service.py` (`_trigger_execution`) | CLOSED | `3e93928` |
| GAP-045 | HIGH | `TaskContract.verification["commands"]` was policy-validated but **never executed** unless a `completion_contract` was also present. Refactored `VerificationService.verify_execution()` so verification commands are always collected and executed regardless of completion_contract presence | REVIEW-007, REVIEW-008 | `services/verification_service.py` | CLOSED | `8c1b8a1` |
| GAP-046 | HIGH | Second attempt (`3743e21`) fixes all three sub-defects, independently re-reproduced: `EventBridge.handle()` now raises on CAS failure (`api/events.py` maps it to `409`, not `204`); the losing event is no longer marked processed, confirmed retryable; and `atomic_transition()` now uses `synchronize_session=False` with the in-memory mutation gated on confirmed success, so a failed call leaves the object showing its real unchanged values instead of a fabricated one | REVIEW-007, REVIEW-008, REVIEW-009 | `services/state_machine.py` (`atomic_transition`), `adapters/macro_agent/event_bridge.py`, `governance_controller/api/events.py` | CLOSED | `3743e21` |
| GAP-047 | HIGH | `POST /events` now validates its body against a real `EventIn` Pydantic schema and has dedicated tests (`tests/test_events_endpoint.py`, including a genuine two-session CAS-race-through-HTTP test) — confirmed by independent reproduction, not just the checked-in suite. The route still has no authentication, same as `POST /approvals` and every other route in the app — not a regression this fix introduced, and already tracked project-wide as `docs/NEXT_STEPS.md`'s Phase 2 "Security hardening / Authentication middleware" candidate task, so not re-litigated here as its own open item | REVIEW-007, REVIEW-008, REVIEW-009 | `governance_controller/api/events.py` | CLOSED | `3743e21` |
| GAP-048 | HIGH | `docker-compose.yml`'s `DATABASE_URL`/`LOG_LEVEL` environment variables lacked the `GC_` prefix `Settings` requires. Renamed to `GC_DATABASE_URL`/`GC_LOG_LEVEL` and pointed the database URL at the `db` service hostname | REVIEW-007 | `src/governance_controller/docker-compose.yml` | CLOSED | `541c83a` |
| GAP-049 | MEDIUM | GAP-032's harness-role enforcement only ran when the harness was registered, so unregistered-but-profile-allowed harnesses skipped validation. Changed `PolicyEngine` to add a violation for any requested harness not present in the registry | REVIEW-007 | `services/policy_engine.py` | CLOSED | `f114a15` |
| GAP-050 | MEDIUM | `specs/SPEC-10-phase-plan.md` and `README.md` still cited `GAP-020`/`GAP-021`/`GAP-022` as open/blocking. Updated the Phase 1 checklist to tick the now-closed durability/verification items and refreshed the footnotes; removed the stale warning from `README.md` | REVIEW-007 | `specs/SPEC-10-phase-plan.md`, `README.md` | CLOSED | `6033a3e` |
| GAP-051 | LOW | Several `TaskContract.execution`/`ProjectProfile.execution` fields were schema-only. Wired `timeout_minutes`, `max_retries`, `sandbox`, and `max_parallel_agents` through `MacroAgentExecutor.start()`'s outbound payload | REVIEW-007 | `schemas/task_contract.py`, `schemas/project_profile.py`, `adapters/macro_agent/executor.py`, `services/approval_service.py` | CLOSED | `c1cc748` |
| GAP-052 | LOW | `Task.version`'s `default=0` was ORM-only with no DDL `server_default`. Added `server_default="0"` to the `version` column | REVIEW-007 | `governance_controller/models/task.py` | CLOSED | `c1cc748` |
| GAP-053 | LOW | `docs/DEPENDENCIES.md` overclaimed `structlog`'s usage, and `schemas/project_profile.py`/`schemas/task_contract.py` both defined unrelated `ExecutionConfig` classes. Corrected the `structlog` description and renamed the project-profile class to `ProjectExecutionConfig` with explicit cross-references | REVIEW-007 | `src/governance_controller/docs/DEPENDENCIES.md`, `schemas/project_profile.py`, `schemas/task_contract.py`, `schemas/__init__.py` | CLOSED | `c1cc748` |
| GAP-054 | MEDIUM | Added the missing `concurrent_modification` audit log call at the `RUNNING`-CAS failure site in `_trigger_execution`, and the commit-before-raise fix in GAP-044 means it now survives `get_db()` rollback — independently reproduced by forcing this exact branch and re-querying with a fresh session | REVIEW-008, REVIEW-009, REVIEW-010 | `services/approval_service.py` (`_trigger_execution`) | CLOSED | `3e93928` |
| GAP-055 | MEDIUM | Assertion restored to an exact `task.version == 3`, correctly reflecting `_trigger_execution`'s 3 CAS increments and confirmed live to catch a double-CAS regression. Note: at the time this was written, `test_executor_failure_advances_to_failed_with_version_3`'s own leftover manual `db.commit()` was masking the fact that production code didn't yet commit there (GAP-044). Production now does commit there (GAP-044 closed by `3e93928`), but the manual `db.commit()` was never removed from the test — see `GAP-056`, which supersedes this note with proof that the leftover commit (and a structural placement bug in two other new tests) still gives zero regression protection | REVIEW-008, REVIEW-009, REVIEW-010 | `tests/test_approval_concurrency.py` | CLOSED | `04a2186` (assertion fixed) |
| GAP-056 | MEDIUM | Third attempt (`345a6e4`) genuinely closes this: renamed the misleading `test_ready_cas_loss_survives_get_db_rollback` to `test_outer_cas_loss_survives_get_db_rollback`, and added dedicated tests for the READY-CAS and RUNNING-CAS sites inside `_trigger_execution`, each monkeypatching `StateMachine.atomic_transition` filtered by `target_state` so it can't cross-contaminate the other sites. Independently re-verified with a 14-trial empirical matrix (each of the 4 commit sites: test passes with the commit present, fails when *that* commit is removed, stays green when a *different* commit is removed instead) — no cross-talk found anywhere, confirmed with hit-counters | REVIEW-010, REVIEW-011, REVIEW-012 | `tests/test_approval_concurrency.py` | CLOSED | `345a6e4` |
| GAP-057 | CRITICAL | `PolicyEngine._is_inside`/`_normalize_path` (`policy_engine.py:94-106`) and `VerificationService._is_prefixed_by` (`verification_service.py:207-211`) both compare paths after only `path.rstrip("/")` — neither calls `os.path.normpath()`/`os.path.expanduser()` to resolve `..` or `~` segments before the prefix/equality check. Live-reproduced: `contract.inputs=["src/../../srv/production/secrets.env"]` against `forbidden_paths=["/srv/production"]` returns `allowed=True, violations=[]` from `PolicyEngine.evaluate()`, and the identical bypass reproduces in `VerificationService.verify_execution()`'s `forbidden_path_check`/`scope_check`. Confirmed directly from source (both functions read; neither does any path canonicalization) independent of the reporting agent's live-repro claim. This defeats the exact control `GAP-002` (also CRITICAL) was written to fix — GAP-002 fixed exact-match vs. prefix-match, but never addressed traversal normalization, in either file | REVIEW-013 | `services/policy_engine.py` (`_is_inside`, `_normalize_path`, `_forbidden_path_conflicts`), `services/verification_service.py` (`_is_prefixed_by`), `utils/paths.py` | CLOSED | `aaca2cc` |
| GAP-058 | HIGH | `VerificationService.verify_and_advance()`'s CAS-failure branch (`verification_service.py:236-252`) calls `AuditService.log(...)` then `raise ValueError(...)` with no `await db.commit()` in between — confirmed directly from source. The same defect class `GAP-044` fixed in `approval_service.py::_trigger_execution`'s three sibling branches, left unfixed in this file. Matters because `EventBridge.handle()` calls `verify_and_advance()` mid-request after already performing its own uncommitted `atomic_transition` earlier in the same call (`event_bridge.py:134-140`); if the second CAS races and loses, the whole request rolls back on `get_db()`'s `except BaseException`, erasing the legitimate first transition and both audit rows, not just the verification failure's own record. Live-reproduced by the reporting agent (seeded two-session race, confirmed 0 audit rows survive rollback). Fixed by `d7ed2a4` and independently confirmed genuinely fixed by direct source reading in REVIEW-014 (`await db.commit()` now sits before the `raise`) — this ledger row was simply never flipped from `OPEN` to `CLOSED` at the time, caught and corrected in REVIEW-016 | REVIEW-013, REVIEW-014, REVIEW-016 | `services/verification_service.py` (`verify_and_advance`) | CLOSED | `d7ed2a4` |
| GAP-059 | HIGH | `PolicyEngine.evaluate()` (`policy_engine.py:268-357`, read end-to-end) never compares `TaskContract.execution.timeout_minutes`/`max_retries` (proposer-controlled) against `ProjectProfile.execution.timeout_minutes` (project-level constraint, SPEC-03 §3.6) — confirmed no such check exists anywhere in the method. `GAP-051` wired these fields through to the outbound executor payload but never validated them against the profile's cap; same "ProjectProfile constraint is schema-only/unenforced" class as `GAP-007`, for a field GAP-007's fix didn't cover | REVIEW-013 | `services/policy_engine.py` (`evaluate`) | CLOSED | `9f64438` |
| GAP-060 | HIGH | `api/tasks.py::create_task()` (confirmed: lines 25-36, no `try/except`) has no exception handling around `TaskService.create()`. A duplicate `task_id` raises a raw `sqlalchemy.exc.IntegrityError` from the `db.add`/`flush()` in `task_service.py`, which propagates as an unhandled 500 with a stack trace instead of `409 Conflict`. Same "unguarded exception → unhandled 500" pattern GAP-005 fixed for `POST /approvals`; never applied to `POST /tasks`. Live-reproduced by the reporting agent against the real ASGI app + `get_db()` (transaction rollback itself is correct — no partial-data loss, purely an API-contract gap). Fixed by `715a50b` and independently confirmed genuinely fixed by live reproduction in REVIEW-014 (duplicate `task_id` now returns `409`, original row survives) — this ledger row was simply never flipped from `OPEN` to `CLOSED` at the time, caught and corrected in REVIEW-016 | REVIEW-013, REVIEW-014, REVIEW-016 | `governance_controller/api/tasks.py` (`create_task`) | CLOSED | `715a50b` |
| GAP-061 | HIGH | `Dockerfile`'s `CMD ["uvicorn", ...]` depends on a package that is declared nowhere — not in `pyproject.toml` `dependencies`, not in `dev`, and absent from all 39 locked packages in `uv.lock`. `docker-compose.yml`'s `api` service builds from this same `Dockerfile`, so `docker compose up` cannot start the API container either. Live-reproduced: `docker build` succeeds, `docker run` fails with `exec: "uvicorn": executable file not found in $PATH`. This is the one documented way to run the Controller in a container (per `docs/NEXT_STEPS.md`) and has been completely broken through all 12 prior review rounds — no round built the actual Docker image, and there is no CI workflow anywhere in the repo that would have caught it | REVIEW-013 | `src/governance_controller/Dockerfile`, `pyproject.toml`, `uv.lock` | CLOSED | `da13b58` |
| GAP-062 | MEDIUM | `POST /events` had no request body size limit. Added `EventBodySizeLimitMiddleware` that buffers the entire ASGI body up to 64 KiB, rejects with `413` if the actual byte count exceeds the cap, and only then hands the buffered messages to FastAPI. This caps chunked/streamed bodies and dishonest `Content-Length` values, not just the declared header; added a regression test that sends a large body without `Content-Length` | REVIEW-013, REVIEW-014 | `governance_controller/api/events.py` (`EventBodySizeLimitMiddleware`) | CLOSED | `e6553e2` |
| GAP-063 | MEDIUM | `services/opentasks_service.py` was never called from `services/`, `api/`, or `adapters/` — orphaned dead code. Removed the stub and its dedicated test; deleted the corresponding "opentasks materialization stub with DAG validation" bullet from `docs/NEXT_STEPS.md` | REVIEW-013 | `governance_controller/services/opentasks_service.py` | CLOSED | `1fa4a52` |
| GAP-064 | MEDIUM | `api/approvals.py` only caught `ValueError`; `ApprovalService.approve()`/`_trigger_execution()` can also raise `RuntimeError` (macro-agent start failure). Added a handler mapping `RuntimeError` to HTTP 503 Service Unavailable | REVIEW-013 | `governance_controller/api/approvals.py` | CLOSED | `a32dd8b` |
| GAP-065 | MEDIUM | `requirements/REQUIREMENTS.md` had drifted from the implementation in 4 ways: `RISK-14` still said GAP-024 was "not yet mitigated" though it's been `CLOSED` since REVIEW-005; `IFR-05` documented a `/webhooks/macro-agent` path that was never implemented (the real route is `/events`); `IFR-02` documented a `POST /tasks/{id}/execute` route that doesn't exist (execution is triggered via `POST /approvals` instead); and no `RISK` row captured the 9-of-56-gap, 9-of-12-round recurring async-session-lifecycle defect class. All four corrected directly in this review (RISK-14 updated, IFR-02/IFR-05 corrected to describe the real interfaces, new RISK-16 added) | REVIEW-013 | `requirements/REQUIREMENTS.md` | CLOSED | REVIEW-013 (direct doc fix) |
| GAP-066 | LOW | `docker-compose.yml`'s `GC_LOG_LEVEL: info` was dead configuration. Added `log_level: str = "info"` to `Settings` and wired it into `structlog.configure()` at module import so the env var is honored | REVIEW-013 | `governance_controller/config.py` | CLOSED | `4cf92b7` |
| GAP-067 | LOW | `EventBridge._EVENT_TO_STATE` only implemented 6 of SPEC-05 §5.4's 9 types. Added `stream:committed`, `mergeQueue:added`, `mergeQueue:ready` as `RUNNING` transitions, with tests | REVIEW-013 | `governance_controller/adapters/macro_agent/event_bridge.py` (`_EVENT_TO_STATE`) | CLOSED | `9471d51` |
| GAP-068 | LOW | `specs/SPEC-10-phase-plan.md`'s `[^phase1-git-cascade]` footnote still described a "deterministic landing stub with branch validation" that was deleted by `0920b65` when `GAP-029`/`GAP-030` were closed — confirmed via `git show --stat 0920b65` and a whole-repo `grep` for `git_cascade`/`GitCascadeService` returning nothing. Corrected directly in this review | REVIEW-013 | `specs/SPEC-10-phase-plan.md` | CLOSED | REVIEW-013 (direct doc fix) |
| GAP-069 | LOW | `docs/NEXT_STEPS.md`'s endpoint bullet omitted `POST /events` despite it being a real, tested, `GAP-047`-closed HTTP route. Corrected directly in this review | REVIEW-013 | `docs/NEXT_STEPS.md` | CLOSED | REVIEW-013 (direct doc fix) |
| GAP-070 | LOW | Commit `8926506`'s message ("chore: remove unintended scheduled_tasks.lock file") doesn't match its actual diff (a 7+/11− fix to a version-assertion in `tests/test_approval_concurrency.py`); no file named `scheduled_tasks.lock` has ever existed in this repo's history. Content is benign; message is a copy-paste/mislabeling artifact. Not fixable without rewriting published history, which is out of scope — noted for the record only | REVIEW-013 | git history (`8926506`) | ACCEPTED — rewriting a published commit message is out of proportion to a benign, already-merged content mismatch; noted for the record | |
| GAP-071 | LOW | `.superpowers/sdd/2026-08-18-phase-1-governance-controller-poc/progress.md` had gone stale again in the exact way `GAP-016` originally flagged and closed: it still stopped at "Review 1"/"Review 2" gap-closure lines, with no entry for Review 3 through Review 13. Backfilled the progress ledger through Review 13 | REVIEW-013 | `.superpowers/sdd/2026-08-18-phase-1-governance-controller-poc/progress.md` | CLOSED | `16d2fe5` |
| GAP-072 | LOW | `e6553e2`'s `EventBodySizeLimitMiddleware` (the genuine GAP-062 byte-counting fix, confirmed correct in REVIEW-015) contains an inline comment marker `# ponytail: buffers the entire body in memory up to the cap; streaming handling would need a different approach...`. Initially flagged as an unexplained artifact (no other occurrence anywhere in the repo, no linked issue/ADR); the user clarified `ponytail` is a known harness skill applied in the neighboring agent's session, not a stray/unintentional marker. No action needed — noted for the record only so a future reader isn't puzzled by the same question | REVIEW-015 | `governance_controller/api/events.py` (`EventBodySizeLimitMiddleware`) | ACCEPTED — intentional marker from a harness skill the user applied in the neighboring agent's session, not an artifact | |
| GAP-073 | MEDIUM | `GAP-062`'s byte-counting `EventBodySizeLimitMiddleware` only applied to `POST /events`. Moved the middleware to a shared `middleware.py` and generalized it to `WriteBodySizeLimitMiddleware`, which caps the body for `/events`, `/tasks`, and `/approvals` by buffering the actual ASGI stream bytes | REVIEW-016 | `governance_controller/middleware.py`, `governance_controller/main.py` | CLOSED | `0add329` |
| GAP-074 | HIGH | `TaskContract.forbidden_paths` was never enforced pre-approval by `PolicyEngine`, and `VerificationService.verify_execution()` only checked it when no `CompletionContract` was attached. Changed `PolicyEngine.evaluate()` to merge `contract.forbidden_paths` into the forbidden-path violation check alongside `profile.security.forbidden_paths`; changed `VerificationService` to always merge task-level forbidden paths with the CompletionContract's list and check them regardless of completion presence | REVIEW-016 | `governance_controller/services/policy_engine.py` (`evaluate`), `governance_controller/services/verification_service.py` (`verify_execution`) | CLOSED | `d31ebd4` |
| GAP-075 | MEDIUM | `specs/SPEC-10-phase-plan.md`'s `[^phase1-verification]`/`[^phase1-durability]`/`[^phase1-policy-bypass]` footnotes still described `GAP-057`/`GAP-058` as `OPEN` and one explicitly claimed `GAP-057` "currently blocks declaring Phase 1 acceptance criteria met" — both gaps were independently live-verified `CLOSED` in REVIEW-014/015, but the file was last touched at the commit that *opened* them (`94f9ad9`) and never updated afterward. Corrected directly in this review, with a cross-reference to the newly-found `GAP-074` in the same area | REVIEW-016 | `specs/SPEC-10-phase-plan.md` | CLOSED | REVIEW-016 (direct doc fix) |
| GAP-076 | MEDIUM | `docs/NEXT_STEPS.md`'s "Deferred to Phase 3+" section listed "Meta Orchestrator integration" and "Intake Adapter" as Phase 3+ work, but `specs/SPEC-10-phase-plan.md` §10.2 explicitly assigns both to Phase 2 ("Meta Orch integration: OpenCode + BMAD + OpenSpec," "Intake adapter (Telegram/Email/API)," "Idea Ingestion Service," "Human Triage queue in Plane"). Moved both into the Phase 2 candidate-task list, corrected directly in this review | REVIEW-016 | `docs/NEXT_STEPS.md` | CLOSED | REVIEW-016 (direct doc fix) |
| GAP-077 | HIGH | SPEC-09 §9.6 "Failure Handling" specifies a retry-before-`FAILED` mechanism with macro-agent failure feedback; `VerificationService.verify_and_advance()` unconditionally transitioned any failed verification straight to `FAILED` with no retry-count tracking. A first fix (`ee80cd6`) added the mechanism but had three bugs (global transition-table scope; event-replay burning a retry; and, tracked separately, `GAP-085`'s finding that the "retry" never actually re-invoked the macro-agent). `f62c5ba` genuinely fixed the first bug and `GAP-085`: `state_machine.py`'s shared table no longer contains `FAILED -> RUNNING` at all (`TaskState.FAILED: set()`); a new, separate `atomic_transition_from_failed_to_running()` is the only call site for that edge — REVIEW-020 live-reproduced delivering all 6 previously-dangerous event types to a `FAILED` task through the real `EventBridge.handle()` and confirmed every one is now correctly rejected, no resurrection. **The event-replay bug is now fixed**: `EventBridge.handle()` records the processed-event dedup key before returning on the retry path, so a replayed `landing:completed` (identical `event_id`) is ignored instead of re-entering the retry path and triggering a second `executor.start()` / second `Execution` row. `test_event_bridge.py::test_landing_completed_failing_contract_retries_and_starts_execution` was extended to call `EventBridge.handle()` twice with the same event and assert `executor.start()` is still awaited only once. REVIEW-021 independently live-reproduced this against a real `EventBridge.handle()`/mocked executor and additionally confirmed no regression: a genuinely distinct subsequent `landing:completed` event (different `event_id`, after a simulated re-landing) still processes normally and reaches `HUMAN_REVIEW`, and the full `max_retries=2` boundary sequence still produces exactly 2 `executor.start()` calls total even when every historical event is replayed afterward | REVIEW-016, REVIEW-019, REVIEW-020, REVIEW-021 | `governance_controller/adapters/macro_agent/event_bridge.py`, `governance_controller/tests/test_event_bridge.py` | CLOSED | `da20f66` |
| GAP-078 | CRITICAL | Every `datetime` column across all 5 models declared a plain `datetime` field with no `sa_column=Column(DateTime(timezone=True))` override, so SQLModel generated `TIMESTAMP WITHOUT TIME ZONE` columns while the codebase produced timezone-aware UTC datetimes. `asyncpg` rejected binding tz-aware values into tz-naive Postgres columns. Fixed by adding `DateTime(timezone=True)` to every datetime field; live-verified by creating a task in a real `postgres:16-alpine` container | REVIEW-016 | `governance_controller/models/task.py`, `models/approval.py`, `models/audit_log.py`, `models/execution.py`, `models/processed_event.py` | CLOSED | `c966da5` |
| GAP-079 | HIGH | `TaskService.create()` used check-then-insert for project profile creation, causing a TOCTOU race when two concurrent `POST /tasks` for different `task_id`s shared a new `project_id`. The loser's `IntegrityError` on the profile insert was indistinguishable from a genuine duplicate task in `api/tasks.py`. Fixed by using dialect-aware `INSERT ... ON CONFLICT DO NOTHING` for the profile row and re-fetching on conflict, plus distinct error messages for task-id vs profile-id conflicts in `api/tasks.py` | REVIEW-016 | `governance_controller/services/task_service.py` (`create`), `governance_controller/api/tasks.py` | CLOSED | `584b989` |
| GAP-080 | HIGH | `TaskService.get_by_id_for_update()` held `SELECT ... FOR UPDATE` across the live macro-agent HTTP call in `_trigger_execution`, blocking concurrent approval attempts on Postgres and contradicting the CAS fail-fast design. `584b989` removed the explicit `get_by_id_for_update()` call (`api/approvals.py` now uses plain `get_by_id()`), but REVIEW-017 found the underlying symptom still reproduced via the natural MVCC row lock from the uncommitted `READY` UPDATE. Fixed by adding `await self.db.commit()` in `_trigger_execution` after the `Execution` row is flushed and before `await self.executor.start(...)` is called, so no open write transaction holds the task row during the live HTTP call. `test_approval_endpoint.py::test_approval_commits_ready_before_macro_agent_start` now asserts a commit occurs before `executor.start()`. REVIEW-018 independently live-reproduced the fix against real Postgres with the exact same timing method REVIEW-016/017 used to catch the first attempt: a probe `SELECT ... FOR UPDATE` from a second connection, issued 0.5s into a 4s-slow mocked executor call, now returns in ~0.02s (3 trials) instead of the ~3.5s block measured before — the row lock is genuinely released. Also verified no durability regression from the new mid-method commit (`expire_on_commit=False` means in-memory `task`/`execution` attributes stay valid; a crash between the commit and the final `RUNNING` transition leaves the task at a sane, recoverable `READY` state, not corruption) | REVIEW-016, REVIEW-017, REVIEW-018 | `governance_controller/services/approval_service.py` (`_trigger_execution`) | CLOSED | `1490371` |
| GAP-081 | MEDIUM | No `RISK` row in `requirements/REQUIREMENTS.md` covers the dialect-parity defect class `GAP-078`/`GAP-079`/`GAP-080` all belong to: SQLite silently tolerates things Postgres rejects or behaves differently on (tz-naive datetimes, `FOR UPDATE`/lock semantics, constraint-name/error-message shapes), and every prior review round's test suite and live reproduction ran exclusively against SQLite until the `GC_TEST_DATABASE_URL` dual-DB infrastructure landed alongside the GAP-078 fix. Compounding factor: there is no CI workflow anywhere in the repo (confirmed — no `.github/workflows`, `.gitea`, `.woodpecker*`), so the new opt-in Postgres test path is never exercised automatically; a future change could silently reintroduce a SQLite-only-passing regression (as `GAP-080` already demonstrates happened within the very same fix cycle that added the dual-DB infra). Added as `RISK-17` directly in this review | REVIEW-017 | `requirements/REQUIREMENTS.md` | CLOSED | REVIEW-017 (direct doc fix, RISK-17) |
| GAP-082 | LOW | `TaskService.get_by_id_for_update()` is now orphaned dead code — its only call site was removed by the `GAP-080` fix attempt. Confirmed via repo-wide grep: the only remaining reference was a test that monkeypatched it specifically to assert it is never called. Deleted the method; the test now asserts the commit-before-start behavior instead. REVIEW-018 confirmed via whole-repo grep that `get_by_id_for_update`/`.with_for_update()`/`FOR UPDATE` no longer appears in any `.py` file | REVIEW-017, REVIEW-018 | `governance_controller/services/task_service.py` (`get_by_id_for_update`) | CLOSED | `1490371` |
| GAP-083 | LOW | `db.py`'s `create_async_engine(settings.database_url, echo=False, future=True)` set no `pool_size`/`max_overflow`/`pool_timeout`/`pool_recycle`/`pool_pre_ping`, and `Settings` exposed no env var to configure any of them. Added `database_pool_size`, `database_max_overflow`, `database_pool_timeout`, and `database_pool_pre_ping` to `Settings` and wired them into `create_async_engine` with sensible defaults (`pool_size=10`, `max_overflow=20`, `pool_timeout=30`, `pool_pre_ping=True`). REVIEW-018 confirmed live: instantiated the engine with all four overridden via env vars and inspected `engine.pool` directly — `size()`/`_max_overflow`/`_timeout`/`_pre_ping` all reflected the overrides, not just declared-and-unused. Cosmetic-only nit: the env var is `GC_DATABASE_MAX_OVERFLOW`, not `GC_DATABASE_POOL_MAX_OVERFLOW` — doesn't match the `GC_DATABASE_POOL_*` prefix pattern the fix commit's message implies, harmless but worth a naming pass if anyone touches this again | REVIEW-017, REVIEW-018 | `governance_controller/db.py`, `governance_controller/config.py` | CLOSED | `1490371` |
| GAP-084 | MEDIUM | Full audit of dual-DB test coverage (prompted by a direct user question) found "the suite runs identically on SQLite and PostgreSQL" is not quite accurate: 6 of `tests/test_db.py`'s 12 tests (`test_get_db_yields_session`, `test_get_db_commits`, `test_get_db_rolls_back_on_exception`, `test_get_db_rolls_back_on_generator_exit`, `test_get_db_commit_branch_requires_exception_catch`, `test_get_db_success_branch_requires_no_exception`) use a locally-defined `in_memory_get_db` fixture that hardcodes `sqlite+aiosqlite:///:memory:` via its own `create_async_engine` call, entirely bypassing the shared `GC_TEST_DATABASE_URL`-aware fixtures in `conftest.py`. Confirmed live: these 6 still pass even when `GC_TEST_DATABASE_URL` points at a Postgres URL with deliberately wrong credentials — proving they never attempt a real connection. Fixed: removed `in_memory_get_db`; rewired the 6 tests to use the shared `patched_db` fixture so `get_db()` actually routes through the configured test database and its commit/rollback logic is exercised against the real dialect. REVIEW-019 independently live-verified against real Postgres: a bad-credentials run makes exactly these 6 (and no others) fail with a real `asyncpg` auth error; a correctly-credentialed run passes all 202; a plain SQLite run also passes all 202 — genuine dual-DB parity, no regression. (Note: this row's `Closed By` previously cited a nonexistent hash `9ae1357`; corrected to the real commit) | REVIEW-018, REVIEW-019 | `src/governance_controller/tests/test_db.py` (`in_memory_get_db` fixture) | CLOSED | `ee80cd6` |
| GAP-085 | HIGH | `ee80cd6`'s `GAP-077` retry branch (`verification_service.py:258-297`) transitions the task `FAILED -> RUNNING` and increments `Task.execution_attempts`, writes an audit row that reads as a legitimate retry, and leaves a `# TODO: emit failure feedback to macro-agent` comment — but **never calls `MacroAgentExecutor.start()` or any equivalent**. Confirmed via repo-wide grep: `executor.start(...)` has exactly one call site in the whole codebase (`approval_service.py:358`, inside `_trigger_execution`, reachable only from the `EXEC_APPROVED -> READY -> RUNNING` approval flow), and `StateMachine`'s transition table has no edge back into `EXEC_APPROVED`/`READY` from anywhere once left, so there is no automatic or API-driven way to re-invoke the macro-agent for a task once its approval flow has run. Live-reproduced twice (direct `verify_and_advance()` call, and through the real `EventBridge.handle()` production entry point with a real failing `TaskContract`): task flips to `RUNNING`, `execution_attempts` increments, a `verification_failed_retry` audit row is written — but zero `Execution` rows are created and zero outbound HTTP calls are made. The task is **silently and permanently orphaned** in `RUNNING` — worse than the pre-fix behavior of going straight to `FAILED`, since `FAILED` was at least terminal and human-actionable, whereas this leaves the task appearing to be actively working forever, with no path to `DONE`, `FAILED`, or manual recovery short of direct database surgery. SPEC-09 §9.6 item 1 ("Controller transitions task to retry") was implemented as a database bookkeeping fiction, not an actual retry. Fixed by `f62c5ba`'s new `_start_retry_execution()`, which creates a real `Execution` row, commits, and calls `executor.start()`. REVIEW-020 live-reproduced the full round trip through `EventBridge.handle()` with a mocked executor: `start()` awaited exactly once per retry, a new `Execution` row created each time, and confirmed the exact `max_retries` boundary (2 retries then terminal `FAILED` with `executor.start()` called exactly twice); also confirmed no `GAP-080`-class regression (the state transition, audit rows, and `Execution` row are committed before `executor.start()` is called — probe against real Postgres returned in 0.02s, not blocked) and CAS safety under a forced two-session race (exactly one racer succeeds, no double-count) | REVIEW-019, REVIEW-020 | `governance_controller/services/verification_service.py` (`verify_and_advance`, `_start_retry_execution`) | CLOSED | `f62c5ba` |
| GAP-086 | HIGH | `WriteBodySizeLimitMiddleware`'s drain loop (`middleware.py:62-67`, added for `GAP-062`/`GAP-073`) only treats `http.request` messages as terminating: `while not complete: tail = await receive(); complete = (tail.get("type") == "http.request" and not tail.get("more_body", False))`. If a client sends a streamed/chunked body exceeding the 64 KiB cap and then disconnects instead of finishing it (an ordinary occurrence — network drop, client abort), `receive()` returns `{"type": "http.disconnect"}`; per the ASGI spec, every subsequent `receive()` call on a disconnected connection immediately returns another disconnect message, so `complete` is never set `True` and the loop spins forever — no response is ever sent, the coroutine never returns, the request is never released. Live-reproduced twice: a mock-`receive()` harness with no suspension point pegged the process at 99.9% CPU until force-killed (confirming a true tight loop, not just a slow task); against the real `governance_controller.main.app` with a properly-yielding mock `receive()`, `asyncio.wait_for(app(...), timeout=4.0)` still timed out. Any unauthenticated caller to `/tasks`, `/approvals`, or `/events` can leak an indefinitely-spinning coroutine per oversized-then-abandoned request — a straightforward DoS. None of `test_body_size_middleware.py`'s 6 tests exercise this path (all use `httpx.AsyncClient`, which always finishes sending its declared body). Fixed by a 2-line change adding an `http.disconnect` check to the drain loop. REVIEW-020 independently confirmed via before/after reproduction: reverted just those 2 lines and observed the same harness call `receive()` over 1 million times in 3 seconds (proving a genuine infinite loop pre-fix and that the test discriminates correctly); against the current code, the same harness returns promptly with no response sent | REVIEW-019, REVIEW-020 | `governance_controller/middleware.py` (`WriteBodySizeLimitMiddleware`) | CLOSED | `f62c5ba` |
| GAP-087 | HIGH | `api/approvals.py::_make_idempotency_key()`'s fallback key (used whenever no `Idempotency-Key` header is supplied — the normal case, since neither the CLI nor the Telegram adapter ever sends one) joined `task_id`/`approval_type`/`actor`/`timestamp` with an unescaped pipe delimiter, and neither `task_id` nor `actor` (both fully caller-controlled free text) were escaped. Two different requests for two different tasks could produce a byte-identical fallback key whenever a literal pipe character in one request's `task_id`/`actor` lined up with the fixed `approval_type` value in between. Live-reproduced against the real ASGI app: task `myid|plan` approved by actor `bob`, and task `myid` approved by actor `plan|bob`, produced identical fallback keys. Submitting the second, entirely separate approval returned `200 {"approved": true}` and logged `approval_idempotent`, but the actual task never left `PROPOSED` — no `Approval` row was created, no state transition occurred, yet the caller was told the approval succeeded. Distinct from the already-closed `GAP-003`/`GAP-004` (header-ignored, missing-dedup-lookup) — this was a delimiter-injection bug in the fallback-key construction those fixes left behind, and it was governance-critical: an approval could be silently dropped with the caller told it succeeded. Fixed by replacing the join with a `sha256` hash of a JSON-encoded tuple. REVIEW-020 independently live-reproduced both the original collision against a reverted copy of the code (confirming the exploit) and its absence against the current code, through the real ASGI app and a real DB session for both tasks, not just the HTTP response | REVIEW-019, REVIEW-020 | `governance_controller/api/approvals.py` (`_make_idempotency_key`) | CLOSED | `f62c5ba` |
| GAP-088 | MEDIUM | `requirements/REQUIREMENTS.md`'s `FR-19` ("Controller shall run deterministic verification and reviewer agent before human review," `P0`, no caveat) overclaims: no reviewer-agent service exists anywhere in the codebase — only harness-role *strings* (`"reviewer"` in `allowed_roles` metadata). Consistent with `SPEC-10`'s own honest `[^phase1-reviewer]` footnote and unchecked "Reviewer can issue a verdict" checklist item, but `FR-19` itself was never annotated for this drift the way `IFR-02`/`IFR-05`/`RISK-14` were under `GAP-065`. Corrected directly in this review: annotated `FR-19` to split "deterministic verification: implemented" from "reviewer agent: deferred to Phase 3, see `SPEC-10` `[^phase1-reviewer]`" | REVIEW-019 | `requirements/REQUIREMENTS.md` | CLOSED | REVIEW-019 (direct doc fix) |
| GAP-089 | MEDIUM | `specs/SPEC-10-phase-plan.md`'s checklist item `[x] Failed verification never produces DONE` ([^phase1-verification]) carries no footnote acknowledging that `GAP-077`'s reopened bugs (the global `FAILED -> RUNNING` transition-table scope, and `GAP-085`'s finding that the "retry" never actually restarts execution) directly threaten this exact guarantee: a resurrected task's `execution_attempts` is never re-checked on the resurrection path, and if a subsequent `landing:completed` happens to pass verification, the task can reach `HUMAN_REVIEW -> DONE` without `max_retries` ever having bounded that resurrection. Added a footnote reference to `GAP-077`/`GAP-085` directly in this review, matching the standard already applied to `[^phase1-durability]`'s treatment of `GAP-080` | REVIEW-019 | `specs/SPEC-10-phase-plan.md` | CLOSED | REVIEW-019 (direct doc fix) |
| GAP-090 | MEDIUM | SPEC-09 §9.6 step 3 ("mark FAILED and alert human") was completely unimplemented and undisclosed — confirmed via repo-wide grep for `alert`/`notif`, zero matches; a terminally `FAILED` task was only discoverable by a human actively polling `GET /tasks/{id}` or the audit log. `f62c5ba` added `VerificationService._alert_human_terminal_failure()`, which writes a real `alert_human` audit-log row (`reason="max_retries_exhausted"`) when a task exhausts its retries, genuinely exercised end-to-end through `EventBridge.handle()` in a new test. REVIEW-020 confirms this is a real, tested, honestly-scoped mechanism for SPEC-09 §9.6's "mark FAILED" half — **but it is not an actual alert**: confirmed via repo-wide grep for `alert|notif|smtp|webhook|slack|email`, the only hit is this one audit-log call site; there is still no outbound notification channel of any kind, so a human must still poll to discover the failure, same discoverability story as before. The code's own docstring honestly discloses this ("Phase 1 has no outbound notification channel; a future Phase 2 notification adapter can subscribe to this event type"). Closed on that basis — an honestly-scoped, tested Phase 1 stub, same pattern as the Plane adapter/CLI stubs — but a real outbound notification channel remains a Phase 2 candidate task, not part of this closure | REVIEW-019, REVIEW-020 | `governance_controller/services/verification_service.py`, `docs/NEXT_STEPS.md` | CLOSED | `f62c5ba` |
| GAP-091 | MEDIUM | `config.py`'s `database_pool_size`/`database_max_overflow`/`database_pool_timeout` (`GAP-083`) were plain `int` fields with no lower-bound validation, and `log_level` (`GAP-066`) accepted any string with a silent fallback to INFO. `f62c5ba` added validators rejecting negative `database_pool_size`/`database_max_overflow` and unrecognized `log_level` values. REVIEW-020 found the fix incomplete: `GC_DATABASE_POOL_SIZE=0` still reproduced the unbounded-pool bug, and `GC_DATABASE_POOL_TIMEOUT=-30` was still silently accepted. Fixed by changing the pool-size/max-overflow validator to require strictly positive values (`> 0`) and adding a non-negative validator for `database_pool_timeout`. New tests in `test_config.py` cover `0`, negative, and negative-timeout cases. REVIEW-021 independently confirmed live: `pool_size=0`/`max_overflow=0` and negative values are rejected, legitimate positive values work, and `pool_timeout=0` is correctly *accepted* (a valid, if aggressive, SQLAlchemy configuration meaning "fail immediately instead of waiting" — semantically distinct from the `pool_size`/`max_overflow` zero-is-unbounded bug class) while `pool_timeout=-30` is correctly rejected | REVIEW-019, REVIEW-020, REVIEW-021 | `governance_controller/config.py` | CLOSED | `da20f66` |
| GAP-092 | LOW | Twice now (REVIEW-019 for `GAP-077`/`GAP-084`'s first fix, and this round for `GAP-085`/`086`/`087`/`090`/`091`'s fix), the coding agent's own ledger edits cited `Closed By` commit hashes that turn out not to be reachable from `main` — `9ae1357` (REVIEW-019, genuinely nonexistent) and `68589d6`/`384ed34`/`d6adb79` (this round — these do exist as raw git objects but are unreachable siblings of the actually-merged `f62c5ba`, same parent `74d2218`, almost certainly leftover amend/rebase artifacts from the agent settling on its final commit). The second form is subtler than the first: `git log --oneline --all` won't show them, but `git cat-file -t <hash>` succeeds, so a reviewer trusting `git log --all` alone (as REVIEW-019 itself did for the first occurrence) could wrongly conclude the hash is fabricated rather than diagnosing it as a dangling amend artifact — and these objects will become truly unfindable the moment `git gc` runs. All five corrected to the real commit hashes directly in this review. Recommend the coding agent verify a hash's presence via `git merge-base --is-ancestor <hash> HEAD` (or equivalent) before writing it into the ledger, not just that some commit with that message exists locally. **Did not recur in REVIEW-021**: the next fix cycle's own ledger update (`2313bab`, closing `GAP-077`/`GAP-091`) cited `da20f66`, confirmed via `git merge-base --is-ancestor da20f66 HEAD` to be a genuine ancestor — the explicit ask to verify hashes before writing them appears to have worked | REVIEW-019, REVIEW-020, REVIEW-021 | `reviews/GAPS.md` | ACCEPTED — a review-process hygiene note about the coding agent's own workflow, not a defect in `governance_controller` itself; no further action beyond the correction already made and this note for future reviewers | |
| GAP-093 | LOW | Initially flagged as a possible concurrency race: two genuinely *concurrent* deliveries of a byte-identical macro-agent event could both pass `EventBridge.handle()`'s dedup `SELECT` before either commits the `ProcessedEvent` row, potentially both entering the retry/verification path. On closer analysis (tracing the actual code path, not just the dedup check in isolation), this is **not exploitable**: both concurrent deliveries also independently attempt the same `atomic_transition(RUNNING -> AGENT_REVIEW)` CAS before ever reaching `verify_and_advance()`/the retry logic — no commit happens between reading the task and that CAS attempt. The second delivery's `UPDATE` blocks on the same row's write lock (real MVCC lock, not just an app-level check) until the first's transaction commits, then re-evaluates its `WHERE version=?/state=?` clause against the now-advanced row and fails (0 rows affected), raising `ValueError: Concurrent modification detected` *before* it can reach the verification/retry code at all. So the existing state-transition CAS already fully serializes concurrent identical events — there is no window where two callers both reach `verify_and_advance()` for the same event. `GAP-077`'s dedup-key fix is correctly scoped to what it actually needed to cover: sequential replay after the fact | REVIEW-021 | `governance_controller/adapters/macro_agent/event_bridge.py` | ACCEPTED — analyzed and confirmed not exploitable given the existing `atomic_transition` CAS; no code change needed | |
| GAP-094 | HIGH | `governance_controller/db.py`'s module-level `engine = create_async_engine(settings.database_url, ..., pool_size=..., max_overflow=..., pool_timeout=..., pool_pre_ping=...)` passes all four pool-tuning kwargs added by `GAP-083`/`GAP-091` unconditionally, regardless of database dialect. `get_db()`'s own code (`if settings.database_url.startswith("sqlite"): await ensure_sqlite_tables()`) treats SQLite as a legitimate, supported runtime configuration (documented and relied on throughout this whole review series as the "SQLite fallback path" for local dev without Postgres/Docker) — but SQLAlchemy's SQLite dialect uses a `StaticPool` that does not accept `pool_size`/`max_overflow`/`pool_timeout` at all. Live-reproduced: `GC_DATABASE_URL=sqlite+aiosqlite:///:memory: uv run python3 -c "import governance_controller.db"` raises `TypeError: Invalid argument(s) 'pool_size','max_overflow','pool_timeout' sent to create_engine()...` — the module fails to import at all, meaning the entire application (any script, the CLI, or `uvicorn governance_controller.main:app`) cannot start under this configuration. This is a real regression introduced by `GAP-083`'s fix and never caught by `GAP-083`/`GAP-091`'s own verification (both rounds tested the pool settings' *validation* logic and Postgres-dialect engine instantiation, but never tried instantiating the actual module-level engine against a real SQLite `database_url`) — the dual-DB test infrastructure (`GC_TEST_DATABASE_URL`) doesn't exercise this either, since it constructs its own engines directly rather than importing `db.py`'s module-level one. Exactly the `RISK-18` defect class (a fix correct for its intended dialect, not audited against the other one this same module has to support). Fix shape: only pass the pool-tuning kwargs when the dialect actually supports them (e.g. skip them for `sqlite`/`StaticPool`, or use `NullPool`/no pool kwargs for SQLite) | REVIEW-021 | `governance_controller/db.py` | OPEN | | 
